<%- include('./partials/header') %>
  <div class="container .container-des mt-5">
    <div class="row">
      <div class="col-md-6">
        <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
          <div class="carousel-inner con">

            <% product.forEach((item, index)=>{%>
              <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                <img src="<%=item.image%>" alt="First slide" style="object-fit: cover; ">
              </div>
              <%}) %>
          </div>



          <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
          </a>
          <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
          </a>
        </div>
      </div>

      <div class="col-md-6 pt-4">
        <h2 class="titulos">
          <%= product[0].nameProduct %>
            <%= product[0].color %>
        </h2>
        <p>
          <%= product[0].description %>
        </p>
        <div class="kontrolDeForma">
          <p><b>Color:</b>
            <%= product[0].color %>
          </p>
          <% if (product[0].size) { %>
            <p><b>Talla:</b>
              <%= product[0].size %>
            </p>
            <% } %>



        </div>
        <button class="link"
          onclick="apiWhatsapp('<%= product[0].nameProduct%> ' ,' <%= product[0].color %>','<%= product[0].size %>')">COTIZAR
          <i class="ri-whatsapp-fill"></i></button>



      </div>
    </div>
  </div>

  <div class="linea"></div>



  <%- include('./partials/footer') %>